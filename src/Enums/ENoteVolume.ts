// A note volume of F or Ff means the note is accented, denote with an accent
// Determines the gain that the playback will use

enum ENoteVolume {
    Pp,
    P,
    Mp,
    Mf,
    F,
    Ff
}
export default ENoteVolume;